# COMPLETE ITEM LIST — 2026-08-09

Supersedes the pre-audit item list. Entries marked **⚠️CORRECTED** were recorded as one thing and
verified as another by the three adversarial audits (2026-08-09). Contribution IDs:
**I** verifiable attribution · **II** instrument-grounded faithfulness · **III** observability
abstention · **IV** grounded-by-construction architecture · **V** negative results.

---

## A. DONE AND VERIFIED

| # | item | summary |
|---|---|---|
| A1 | Attribution sign bug | One-sided specificity penalised physically-correct anti-aligned maps. Fixed → `src/eval/attribution_metrics.py`, 11 tests |
| A2 | Re-scored maps | **⚠️CORRECTED**: above permutation null on 11/11, but honest range is **4%–97% of ceiling**, NOT "40–55%" (srmr 4% with CI including zero; overlap_ratio 97% = input echo) |
| A3 | Map figures | `scripts/plot_attribution_maps.py` + PNGs |
| A4 | Wasted map reads | Loading gated on lambda; 16 configs, ~41.7k reads/epoch saved |
| A5 | logvar gate (lsm) | Uncertainty rises under overlap, up to 0.745 |
| A6 | Verified-slot eval | lsm 0.6927 on full 6000, bootstrap CIs |
| A7 | Matched 3-arm re-score | Reproduced digit-for-digit under one identical command on provably identical clip sets |
| A8 | Arm decision | attn-concat 0.7236, nMAE 0.4723, seed spread 0.0036. **⚠️CORRECTED**: 0.7246 is film-attn on **fw2**, 0.7236 is attn-concat on **fw** — NOT target-matched. Verified-slot survives (aux GT is CSV scalars); free-decode comparison does NOT |
| A9 | Readout confound | val = free decode (200 clips, max-over-epochs), test = verified-slot (6000). Selection-overfit claim retracted |
| A10 | `N_AUX_FEATURES` | 13 → 11 (verified at source; stale docstring remains at `adapter.py:614`) |
| A11 | FiLM bug | Doc said "open"; fixed 2026-04-26 (`09f6db4`). All ablations post-date it and stand |
| A12 | lsm6 | val 0.7600 — inside the 4-epoch seed range |
| A13 | Doc hygiene | EXPLAINABILITY §1.5–1.28, STATE, CLAUDE.md, SYNTHESIS, PLAN, memory |
| A14 | Four-way re-pool | `\|z\|` costs −0.050 on a good representation |
| A15 | logvar on arm of record | Transfers and strengthens — hnr 0.849, shimmer 0.789; **9 of 11 positive** (not 8), stable at n=48 |
| A16 | Re-mix deletion | Protocol built (in-distribution); **NULL** — no feature's win-rate CI excludes 0.5 |
| A17 | ERF | Architectural ~1 s, not per-feature |
| A18 | Ceiling CI | 0.2594 [0.2513, 0.2681] |
| A19 | `srcc_mean_all` | Renamed `_diag_..._LEAKY` |
| A20 | Graded sweep + blind | rho ≈ 1.000 on 8/9; **6/9 survive blinding**; f0 collapses to −0.900 (was reading its oracle input) |
| A21 | Overlap ablation | −0.017 headline, +0.112 f0, ill-posed +0.026 |
| A22 | Layer sweep | L7 **0.7035** vs L24 0.6887 (+0.0148 headline, **+0.0650 ill-posed**); sanity drift 0.0004; top3-concat WORSE (−0.0145) |
| A23 | Probe family | Tuning verified honest (RidgeCV cv=5, train only). **⚠️CORRECTED: tuned L7 ridge BEATS us by 0.0134** — the recorded "+0.0031" compared against the UNTUNED ridge |
| A24 | Mel baseline | Beats us on snr/hnr; we win temporal 0.05–0.26 |
| A25 | Audio-only aux | **⚠️CORRECTED**: honest retrained arm is **0.7109**, not the mislabeled 0.7066 (an off-distribution lower bound). Matched-L24 margins +0.0226 untuned / +0.0070 tuned |
| A26 | AURC, both definitions | Continuous on arm of record: robust5 **+19.9%**, ill-posed **+12.2%**, srmr +38.3%. Binary on fw2: ill-posed +18.6% |
| A27 | Arm CIs | film-attn − attn-concat **+0.0010 [−0.0017, +0.0037]** (tie); attn-concat − lsm **+0.0309 [+0.0269, +0.0352]** |
| A28 | Provenance settled | `overlap_info` is ORACLE stem-VAD: rebuild bit-identical **45/45**, pyannote **0/45**. CLAUDE.md Pyannote section was FALSE |
| A29 | snr_db source proof | ⚠️**CORRECTED 2026-08-11: the second half is BACKWARDS.** The label IS `10log10(Σxr²/Σ(g·noise)²)` to ~4e-9 dB, so snr is frame-decomposable AND the **most** causally testable feature (scaling noise by `c` gives new GT `snr_db − 20log10(c)` in CLOSED FORM, zero instrument noise). See PLAN §SNR RE-INCLUSION |
| A30 | Twin identity | 9/11 features identical on ALL 3000 test + 3000 dev + 13,900 train pairs. Effective unique GT 13,900/3,000/3,000 |
| A31 | Decode-gate audit | Gate is a hard-coded oracle rule, but fires ONLY on the 5 ill-posed features; **30000/30000 robust5 slots emit the aux value** ⇒ headline is mathematically gate-independent |
| A32 | fw supervision hole | fw has **0 of 19,900** clean-clip f0/hnr/shimmer values. LM voice panel structurally untestable on every arm but the fw2 headline |
| A33 | Oracle-map design | 10/11 features admit exact maps; ceilings measured (f0 0.743, f0_sd 0.656, overlap 0.932); evidence-vs-overlap correlation **−0.389** |
| A34 | Sigma is condition-level | rho 1.000 across conditions, **0.05–0.26 within** condition |
| A35 | Storage cleanup | 405 GB → **1,456 GB** free (1,051 GB reclaimed) |
| A36 | Corrections | Tan et al. citation (time-series, not speech); Kuan & Lee split; margin +0.040 → **+0.018**; "+79%" vacuous (6 error events, mean was `nan`); 0.6986 row is 5,650 clips |

## A2. DONE 2026-08-10 (contribution I build-out)

| # | item | summary |
|---|---|---|
| 0.1 | U-1 f0 provenance | **RESOLVED from source**: f0 GT = Praat on the MIXTURE, `keep = voiced & ~in_overlap` (`f0_clean.py:69`), 100 Hz. `clean_f0` means clean FRAMES, not clean stem |
| 0.6 | oracle maps built | f0_mean, f0_sd, overlap_ratio + per-frame `q`/`keep`; 5849 test clips, errors=0 |
| 0.6b | **exactness VERIFIED** | additive reconstruction 1.7e-13; analytic vs finite-diff 2.6e-10. "Exact" is now tested, not asserted |
| 0.3 | ceiling curves | 6.25 Hz: f0_mean 0.668, f0_sd 0.551, overlap 0.883. 100 Hz returns exactly 1.000 (sanity) |
| 0.9 | nulls N1-N5 | N1/N4 at zero with CIs containing zero; **N5 splits features**: f0_mean −0.467 (|N5| huge), f0_sd +0.032 |
| 0.10 | clean-clip panel | recovered (was silently dropping 3000 clips to a sibling audio dir) |
| 0.17 | noise ladder | monotone; f0_mean PLATEAUS (binary map) -> motivated 0.30 |
| 0.18 | **positive control PASSES** | perfect defining-statistic model returns **0.7310 = the ceiling exactly**. Support errors dominate value errors (10% voicing = −0.27; 30% f0 = −0.10) |
| 0.8/0.8b | saliency: grad, grad×input, IG | all 6 captures with `state_dict=EXACT MATCH` |
| 0.14 | overlap_ratio vs floor | **mixtures gain −4.8% vs constant predictor** (GT sd=0.095). Stays OUT of the headline; pooled +79.9% is bimodality |
| 0.15 | fw2 configs | created `.fw2.yaml` for 5 arms; historical configs left intact to preserve provenance |
| — | **storage** | 405 GB -> 1,456 GB free (1,051 GB reclaimed) |
| — | **⚠️ RETRACTION** | random-head bug invalidated all §1.31 model numbers; bug class fixed in 5 scripts; deletion-grounding null now invalid on 3 grounds |

## B. ANSWERED, NO ACTION

| # | item |
|---|---|
| B1 | Mel-spectrogram is 2-D but unused; WavLM eats raw waveform |
| B2 | Gradient ban covers only spectrogram-through-frozen-encoder |
| B3 | attention vs 1-D vs 2-D are orthogonal axes |
| B4 | `srcc_robust` vs `srcc_all` coincide because two errors cancel |
| B5 | Ceilings: report raw + null + ceiling |
| B6 | Wang et al. category error (verified from primary source) |
| B7 | Observability weighting ≠ decoupled gate |
| B8 | Zero-init gate = mean pooling at init |
| B9 | FiLM null stands (measured with a working FiLM) |
| B10 | All 4 maps are 1-D time maps; srmr admits 2-D; attention is a different object |
| B11 | Effective sample size is half the clip count (twin duplication) |

## C. PHASE 0 — CPU ONLY, NO RETRAINING (see PLAN_2026-08-09.md)

0.1 U-1 f0 provenance (**BLOCKS all scoring**) · 0.2 zeroed-overlap control · 0.3 ceiling curves
all features × rate grid · 0.4 oracle error-predictor ceiling · 0.5 event dumps · 0.6 build oracle
maps · 0.7 srmr rebuild · 0.8 gradient saliency · 0.9 N5 null · 0.10 clean-clip panel ·
0.11 evidence-region sigma pooling · 0.12 within-bin AURC · 0.13 Pyannote baseline
(**re-costed: needs re-run on matched audio**) · 0.14 re-score overlap_ratio vs floor ·
0.15 `fw`→`fw2` · 0.16 calibrated per-feature sigma gate

## D. PHASE 1 — ALL FIXES BEFORE ANY RETRAIN (reordered 2026-08-09)

| id | item | why it must precede training |
|---|---|---|
| 1.1 | **Graded-overlap regeneration** | Training overlap is bimodal {0} ∪ [0.5,1]; retraining before this wastes the run |
| 1.2 | `augment_reverb.py` (never run) | Makes srmr independent instead of a pitch proxy |
| 1.3 | `fw`→`fw2` applied | Else we retrain with zero voice supervision again |
| 1.4 | L7 activation normalization | L7 is ~20× larger than L24 and nothing normalizes |
| 1.5 | Drop dead overlap-conditioning path | Audio-only arm carries a path fed constant zeros |
| 1.6 | Graded sweep on EXISTING audio-only ckpt | An evaluation, not a retrain — its result designs the retrain |

## E. PHASE 2 — RETRAIN ONCE, ON FULLY-FIXED DATA (44 SU)

Ranked: audio-only tuned arm · fw2 shipping arm · defining-statistic pooling (IV) · L7 arm ·
measurement-path token-rate sweep · residual-prediction head · MDN.
Plus the separate prefix-length ablation scored on BLEU/hedge accuracy, **not** `srcc_robust`.

## F. SMALL / FREE, STILL OPEN

F1 per-feature reference signals (targets on disk, no consumer) · F2 continuous overlap target
re-score · F3 sufficiency certificates · F4 abstention scope wording · F5 test.csv sentinel ·
F6 retired band-F1 naming · F7 SFS naming · F8 nMAE bias/variance decomposition · F9 wandb re-key ·
F10 f0_mean median/paired test · F11 stale docstring `adapter.py:614` · F12 project rename ·
F13 checkpoint optimizer-state stripping (614 GB)

## G. DEFERRED / DROPPED

**Deferred:** Liu · entmax · SRMR 2-D (only the time-resolved variant could support it) ·
dense supervision · listenable explanations · f0 2-D
**Dropped:** conformal framing (0/15 certified) · overlap map (soft-IoU 0.259 vs 0.226 random) ·
averaged SRMR map (specificity mathematically capped) · "grounding succeeds for local quantities"
· frame-level abstention wording · full fine-tuning · human metric validation · IRM ·
MoE loss routing · top3-concat
